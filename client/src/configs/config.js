/**
 * Порт запуска API приложения.
 * @type {number}
 */
const backendPort = 81;

/**
 * Домен, на котором располагается API приложения.
 * Указывается без протокола и закрывающего слеша.
 * @type {string}
 */
const apiHost = "nyashmyash99.ru"

module.exports = { apiHost, backendPort };
